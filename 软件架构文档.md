# Deep Knowledge Search 软件架构文档

## 概述

Deep Knowledge Search（深度知识搜索）是一个基于大语言模型（LLM）的智能任务分解与执行系统。系统接收复杂的用户任务描述，通过LLM递归拆解为可执行的子任务，按照并行或串行模式执行，验证结果质量，并生成结构化输出文档。

**核心价值**：将复杂的知识探索任务自动转化为系统化的研究流程，降低用户操作成本，提升任务执行的一致性和可追溯性。

## 系统架构

### 架构全景图

```
┌─────────────────────────────────────────────────────────────┐
│                     用户接口层                               │
├─────────────────────────────────────────────────────────────┤
│  • CLI命令行接口 (交互式/批处理模式)                          │
│  • Web Dashboard (实时任务监控)                              │
│  • RESTful API (任务管理接口)                                │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     业务逻辑层                               │
├─────────────────────────────────────────────────────────────┤
│  • 任务代理 (Agent) - 任务编排核心                             │
│  • 任务规划器 (Planner) - LLM驱动的任务分解                    │
│  • 任务执行器 (Executor) - 递归任务执行引擎                     │
│  • 结果验证器 (Verifier) - 迭代式结果质量评估                  │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     服务层                                   │
├─────────────────────────────────────────────────────────────┤
│  • LLM服务 (多模型支持、API调用、工具调用)                     │
│  • MCP工具服务 (模型上下文协议、工具注册与执行)                │
│  • 配置服务 (统一配置管理)                                    │
│  • 存储服务 (日志、检查点、文档持久化)                        │
└─────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────────────────────────────────────┐
│                     基础设施层                               │
├─────────────────────────────────────────────────────────────┤
│  • 文件系统 (层级化文档存储)                                  │
│  • HTTP/WebSocket通信                                         │
│  • 并发控制 (goroutine、互斥锁)                               │
│  • 时间调度 (定时检查点、超时控制)                            │
└─────────────────────────────────────────────────────────────┘
```

### 技术栈

- **语言**: Go 1.21+
- **主要依赖**:
  - `github.com/chzyer/readline` - 交互式命令行
  - `github.com/google/uuid` - UUID生成
  - `github.com/gorilla/websocket` - WebSocket通信
  - 标准库: `net/http`, `sync`, `context`, `time`

## 核心模块详解

### 1. Agent模块 (`agent/`)

#### 1.1 任务节点 (TaskNode)
**文件**: `agent/task_node.go`

**职责**: 定义任务树的基本数据结构，支持递归嵌套。

**关键数据结构**:
```go
type TaskNode struct {
    ID       string
    ParentID string
    Depth    int
    
    // 任务描述
    Title       string
    Description string
    Goal        string
    
    // 执行配置
    ExecutionMode ExecutionMode // "sequential" | "parallel"
    MaxRetries    int
    CanDecompose  bool
    
    // 树结构
    Children []*TaskNode
    
    // 状态跟踪
    Status   NodeStatus // "pending" | "running" | "paused" | "done" | "failed" | "canceled"
    Progress float64
    
    // 上下文与结果
    Context *TaskContext
    Result  *TaskResult
    
    // 验证信息
    Verification *VerificationInfo
    
    // 时间戳
    CreatedAt  time.Time
    StartedAt  *time.Time
    FinishedAt *time.Time
    
    // 日志记录
    Logs     []ExecutionLog
    LLMCalls []LLMCallRecord
    
    // 并发控制
    mu       sync.RWMutex
    cancelCh chan struct{}
}
```

**设计特点**:
- 线程安全设计（`sync.RWMutex`）
- 支持取消和暂停机制
- 完整的生命周期记录
- 父子节点引用关系

#### 1.2 任务执行器 (TaskExecutor)
**文件**: `agent/executor.go`

**职责**: 协调任务树的执行流程，实现递归分解、并行/串行执行、结果聚合。

**核心算法**:
```
executeNode(node):
  1. 设置节点输出路径
  2. 检查暂停/取消状态
  3. 判断是否需要分解（深度<MaxDepth && CanDecompose）
  4. 如果需要分解:
     - 调用LLM进行任务规划
     - 创建子节点
     - 确定执行模式（parallel/sequential）
  5. 如果有子节点:
     - 根据执行模式执行子节点
     - 聚合子节点结果
  6. 如果是叶子节点:
     - 调用LLM执行任务（支持工具调用）
  7. 标记节点完成
  8. 保存进度和检查点
```

**关键特性**:
- **递归执行**: 最大深度可配置（默认3层）
- **并行控制**: 支持并行和串行执行模式
- **进度跟踪**: 实时进度计算和广播
- **错误处理**: 重试机制（默认3次）
- **检查点**: 每30秒自动保存，支持任务恢复

#### 1.3 任务规划器 (TaskPlanner)
**文件**: `agent/planner.go`

**职责**: 与LLM交互，完成任务的智能分解、执行和验证。

**四大核心功能**:
1. **PlanNode**: 将复杂任务分解为子任务
2. **ExecuteNode**: 执行叶子节点任务（调用工具）
3. **SynthesizeResults**: 聚合子任务结果
4. **VerifyResult**: 迭代验证结果质量（最多5次）

**验证流程**:
```
for iteration in 1..5:
  1. 调用LLM验证当前结果
  2. 如果通过（响应包含"VERIFICATION_PASSED"）:
     - 记录验证通过
     - 返回成功
  3. 如果未通过:
     - 记录反馈
     - 调用LLM根据反馈改进结果
     - 继续下一轮验证
```

#### 1.4 显示模块 (Display)
**文件**: `agent/display.go`

**职责**: 统一管理控制台输出和WebSocket广播，实现前后端状态同步。

**双通道输出**:
- **控制台**: 彩色格式化输出，实时显示任务进度
- **WebSocket**: JSON格式状态广播，支持Web Dashboard实时更新

#### 1.5 日志存储 (LogStorage)
**文件**: `agent/log_storage.go`

**职责**: 持久化任务执行记录，生成结构化输出。

**输出结构**:
```
output/{任务标题_时间戳}/
├── doc/                    # 任务产出文档（树形目录结构）
│   ├── 子任务1/
│   │   ├── 文档1_时间戳.md
│   │   └── 文档2_时间戳.md
│   └── 子任务2/
│       └── 文档3_时间戳.md
├── logs/                   # 执行日志
│   ├── checkpoint.json    # 检查点（支持任务恢复）
│   ├── execution.json     # 完整执行记录
│   ├── progress.json      # 进度历史
│   └── feature_list.json  # 特性清单
└── README.md              # 自动生成的索引文档
```

#### 1.6 恢复管理 (Recovery)
**文件**: `agent/recovery.go`

**职责**: 管理任务的中断恢复，支持从检查点继续执行。

**恢复流程**:
1. 扫描`output/`目录查找未完成的任务
2. 加载检查点文件重建任务树
3. 重新创建执行器继续执行

### 2. LLM模块 (`llm/`)

#### 2.1 客户端 (Client)
**文件**: `llm/client.go`

**职责**: 封装LLM API调用，支持工具调用和消息压缩。

**工具调用循环**:
```go
for iteration := 0; iteration < 30; iteration++ {
  1. 压缩消息历史（避免token超限）
  2. 发送请求到LLM API
  3. 如果响应包含工具调用:
     - 执行工具
     - 将结果添加到消息历史
     - 继续下一轮迭代
  4. 如果无工具调用:
     - 返回最终响应
}
```

**关键特性**:
- **消息压缩**: 智能压缩长对话历史
- **重试机制**: HTTP错误自动重试
- **超时控制**: 1小时超时设置
- **工具调用**: 支持多轮工具调用（最多30轮）

#### 2.2 配置管理 (Config)
**文件**: `llm/config.go`

**职责**: 管理多模型配置，支持运行时模型切换。

**配置结构**:
```json
{
  "models": [
    {
      "name": "deepseek",
      "api_key": "sk-...",
      "base_url": "https://api.deepseek.com/v1/chat/completions",
      "model": "deepseek-chat",
      "temperature": 1.0
    },
    {
      "name": "glm4.7",
      "api_key": "...",
      "base_url": "https://open.bigmodel.cn/api/paas/v4/chat/completions",
      "model": "glm-4.7",
      "temperature": 1.0
    }
  ],
  "default_model": "deepseek"
}
```

**配置优先级**:
1. 配置文件 (`config.json`)
2. 环境变量 (`OPENAI_API_KEY`, `DEEPSEEK_API_KEY`)
3. 默认值 (DeepSeek API)

### 3. MCP模块 (`mcp/`)

#### 3.1 MCP核心 (MCP)
**文件**: `mcp/mcp.go`

**职责**: 实现模型上下文协议（Model Context Protocol），提供工具注册和调用框架。

**核心机制**:
- **工具注册表**: 全局工具注册表，支持动态注册
- **上下文传递**: 通过`context.Context`传递输出路径等元数据
- **统一接口**: 标准化工具调用和响应格式

#### 3.2 工具实现 (Tools)
**文件**: `mcp/tools.go`

**当前支持的工具**:
- **saveToDisk**: 保存内容到文件系统

**工具调用上下文**:
```go
// 从上下文获取输出路径
outputDir, ok := ctx.Value(ContextKeyOutputPath).(string)
```

**扩展性**: 可通过`RegisterTool()`函数轻松添加新工具。

### 4. Web模块 (`web/`)

#### 4.1 HTTP服务器 (Server)
**文件**: `web/server.go`

**职责**: 提供Web Dashboard和REST API。

**API端点**:
- `GET /` - Dashboard页面
- `GET /ws` - WebSocket连接
- `GET /api/status` - 服务状态
- `GET /api/history` - 任务历史列表
- `GET /api/history/{id}` - 任务详情
- `GET /api/docs` - 文档列表
- `GET /api/docs/{path}` - 文档内容
- `POST /api/task/pause/{id}` - 暂停任务
- `POST /api/task/resume/{id}` - 恢复任务
- `GET /api/task/recoverable` - 可恢复任务列表
- `POST /api/task/recover/{id}` - 恢复任务

#### 4.2 WebSocket中心 (Hub)
**文件**: `web/hub.go`

**职责**: 管理WebSocket连接，实现实时状态广播。

**特性**:
- 连接管理（注册、注销）
- 消息广播（支持新连接获取最新状态）
- 心跳检测（30秒Ping/Pong）

### 5. 配置模块 (`config/`)

#### 5.1 统一配置 (Config)
**文件**: `config/config.go`

**职责**: 提供统一的配置管理，支持多模型配置和向后兼容。

**配置加载策略**:
1. 当前目录的`config.json`
2. 可执行文件同目录的`config.json`
3. 用户目录的`~/.dks/config.json`
4. 环境变量回退

## 任务执行流程

### 完整执行序列

```
┌─────────┐    ┌──────────┐    ┌─────────────┐    ┌─────────────┐
│用户输入  │───▶│创建根节点│───▶│初始化执行器 │───▶│执行预检阶段 │
└─────────┘    └──────────┘    └─────────────┘    └─────────────┘
                                                           │
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    │
│生成特性清单  │◀───│加载进度历史 │◀───│递归执行节点 │◀───┘
└─────────────┘    └─────────────┘    └─────────────┘
                           │
                    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
                    │节点需分解？  │───▶│LLM任务规划  │───▶│创建子节点   │
                    └─────────────┘    └─────────────┘    └─────────────┘
                           │                    │
                    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
                    │叶子节点执行 │◀───│执行子节点   │◀───│确定执行模式 │
                    └─────────────┘    └─────────────┘    └─────────────┘
                           │
                    ┌─────────────┐    ┌─────────────┐    ┌─────────────┐
                    │工具调用循环 │───▶│保存结果     │───▶│聚合子结果   │
                    └─────────────┘    └─────────────┘    └─────────────┘
                                                           │
┌─────────────┐    ┌─────────────┐    ┌─────────────┐    │
│验证结果质量 │◀───│生成输出索引 │◀───│清理检查点   │◀───┘
└─────────────┘    └─────────────┘    └─────────────┘
       │
┌─────────────┐    ┌─────────────┐
│验证通过？   │───▶│任务完成     │
└─────────────┘    └─────────────┘
```

### 数据流

```
用户输入 → TaskNode → TaskExecutor → TaskPlanner → LLM API
                                    ↓
                                  MCP Tools → 文件系统
                                    ↓
                            执行结果 → 验证循环
                                    ↓
                             输出文档 + 日志
```

## 设计模式与架构决策

### 1. 组合模式 (Composite Pattern)
**应用**: 任务树结构 (`TaskNode` 包含 `Children []*TaskNode`)
**优势**: 统一处理单个任务和任务集合，支持递归操作

### 2. 命令模式 (Command Pattern)
**应用**: `TaskExecutor` 封装任务执行逻辑
**优势**: 将请求封装为对象，支持排队、日志、撤销（暂停/恢复）

### 3. 观察者模式 (Observer Pattern)
**应用**: WebSocket广播系统 (`Hub` 和 `Client`)
**优势**: 实现松耦合的事件通知，支持多个Dashboard客户端

### 4. 策略模式 (Strategy Pattern)
**应用**: 并行/串行执行策略 (`ExecutionMode`)
**优势**: 运行时动态切换执行策略，提高系统灵活性

### 5. 模板方法模式 (Template Method Pattern)
**应用**: `executeNode()` 中的固定执行流程
**优势**: 定义算法骨架，允许子步骤变化（分解、执行、聚合）

### 6. 工厂模式 (Factory Pattern)
**应用**: MCP工具注册 (`RegisterTool`)
**优势**: 统一工具创建接口，支持动态扩展

### 7. 上下文模式 (Context Pattern)
**应用**: `context.Context` 传递输出路径和任务元数据
**优势**: 避免全局变量，支持请求级别的数据传递

## 关键架构决策

### 1. 递归任务分解
**决策**: 采用深度优先的递归分解策略
**理由**: 
- 符合人类解决复杂问题的思维模式
- 支持任意深度的任务细化
- 便于结果聚合和验证

**限制**: 最大深度3层，避免无限递归

### 2. 检查点与恢复
**决策**: 实现基于JSON的检查点系统
**理由**:
- 支持长时间任务的断点续执行
- 提高系统容错性
- 便于调试和监控

**实现**: 每30秒自动保存，任务暂停时强制保存

### 3. 多模型支持
**决策**: 抽象LLM配置层，支持运行时切换
**理由**:
- 避免供应商锁定
- 支持模型性能对比
- 提高系统可用性（故障转移）

### 4. 工具调用集成
**决策**: 实现MCP（Model Context Protocol）子集
**理由**:
- 标准化LLM与工具交互
- 支持工具动态注册
- 提供上下文感知的工具执行

### 5. 分层输出目录
**决策**: 按任务树结构组织输出文件
**理由**:
- 保持输出结构与任务结构一致
- 便于用户导航和查找
- 支持自动化文档索引生成

### 6. 实时状态同步
**决策**: WebSocket双向通信
**理由**:
- 低延迟状态更新
- 支持多客户端同时监控
- 无需轮询，减少服务器负载

## 并发与线程安全

### 线程安全设计
1. **TaskNode**: 使用 `sync.RWMutex` 保护状态变更
2. **TaskExecutor**: 独立的暂停/恢复控制通道
3. **MCP工具注册表**: 读写锁保护并发访问
4. **WebSocket Hub**: 互斥锁保护客户端连接

### 并行执行控制
- **goroutine管理**: 每个并行子任务在独立goroutine中执行
- **错误收集**: 使用通道收集子任务错误
- **等待组**: `sync.WaitGroup` 同步并行任务完成

## 错误处理与容错

### 重试策略
1. **LLM API调用**: 3次重试，指数退避
2. **节点执行失败**: 节点级别重试（`MaxRetries` 配置）
3. **工具调用失败**: 返回错误信息，由LLM决定下一步

### 故障隔离
1. **节点隔离**: 单个节点失败不影响兄弟节点
2. **子任务隔离**: 并行执行的子任务相互独立
3. **检查点恢复**: 任务失败后可从最近检查点恢复

### 监控与日志
1. **结构化日志**: JSON格式执行日志
2. **实时进度**: 控制台和WebSocket双重输出
3. **详细错误记录**: 包含错误上下文和堆栈信息

## 部署与扩展

### 部署架构
```
单机部署（当前）:
  应用进程 → 本地文件系统 → LLM API (远程)

未来扩展方向:
  ┌─────────┐    ┌──────────┐    ┌──────────┐
  │负载均衡器│───▶│应用服务器│───▶│共享存储  │
  └─────────┘    └──────────┘    └──────────┘
         │              │              │
    ┌─────────┐    ┌──────────┐    ┌──────────┐
    │Dashboard│    │任务队列   │    │对象存储  │
    └─────────┘    └──────────┘    └──────────┘
```

### 扩展性考虑
1. **水平扩展**: 无状态任务执行，支持多实例部署
2. **存储分离**: 输出目录可配置为网络存储（NFS、S3）
3. **队列集成**: 可集成消息队列实现任务异步处理
4. **认证授权**: Web API可添加JWT认证

### 性能优化
1. **并发控制**: 限制并行任务数量，避免资源耗尽
2. **内存管理**: 消息压缩避免token超限
3. **文件IO**: 异步日志写入，减少阻塞
4. **连接池**: HTTP客户端复用连接

## 安全考虑

### 输入验证
1. **文件路径**: 防止路径遍历攻击
2. **用户输入**: 任务描述长度限制
3. **API密钥**: 配置文件权限控制

### 输出安全
1. **内容过滤**: 工具输出内容检查
2. **文件权限**: 输出文件权限设置（0644）
3. **目录隔离**: 每个任务独立输出目录

### 网络安全
1. **WebSocket**: 支持CORS配置
2. **HTTP服务**: 可添加TLS支持
3. **API限流**: 可实施请求频率限制

## 监控与运维

### 健康检查
1. **API端点**: `/api/status` 返回服务状态
2. **资源监控**: 内存、CPU使用情况
3. **任务队列**: 正在执行和排队的任务数量

### 日志分析
1. **执行日志**: `logs/execution.json` 完整记录
2. **错误统计**: 按错误类型分类统计
3. **性能指标**: LLM调用耗时、工具执行时间

### 备份策略
1. **配置备份**: 配置文件版本控制
2. **输出归档**: 旧任务输出可定期归档
3. **检查点清理**: 成功任务自动清理检查点

## 限制与已知问题

### 当前限制
1. **工具支持有限**: 目前仅支持 `saveToDisk` 工具
2. **内存使用**: 大型任务树可能占用较多内存
3. **网络依赖**: 强依赖LLM API可用性
4. **单机部署**: 不支持分布式任务执行

### 已知问题
1. **中文路径处理**: 文件名清理可能破坏中文字符
2. **长任务中断**: 无任务优先级和抢占机制
3. **资源竞争**: 并行任务可能竞争同一文件

## 未来演进方向

### 短期改进
1. **更多工具**: 添加Web搜索、数据库查询等工具
2. **插件系统**: 支持第三方工具插件
3. **任务模板**: 预定义任务模板库

### 长期规划
1. **分布式执行**: 支持多节点任务分发
2. **模型微调**: 任务特定模型微调支持
3. **协作功能**: 多用户任务协作和分享
4. **知识库集成**: 与现有知识库系统集成

---

**文档版本**: 1.0  
**最后更新**: 2026年2月28日  
**对应代码版本**: Deep Knowledge Search v1.0.0